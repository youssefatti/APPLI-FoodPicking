import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    // backgroundColor: "red",
    flex: 1,
    alignItems: "center",
    justifyContent: "space-around"
  }
});
